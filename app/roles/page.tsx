"use client";
import { useCallback, useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
type Role = { id: string; name: string; description: string; manageUsers: boolean; manageAttendance: boolean; viewAttendanceReports: boolean; status: "active" | "inactive" };
const empty: Role = { id: "", name: "", description: "", manageUsers: false, manageAttendance: false, viewAttendanceReports: false, status: "active" };
export default function RolesPage() { const [roles,setRoles]=useState<Role[]>([]),[form,setForm]=useState<Role>(empty),[message,setMessage]=useState(""),[busy,setBusy]=useState(true); const load=useCallback(async()=>{setBusy(true);const response=await fetch("/api/roles",{cache:"no-store"});const result=await response.json();setMessage(response.ok?"":result.message);setRoles(result.roles??[]);setBusy(false);},[]); useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer)},[load]); async function save(event:React.FormEvent){event.preventDefault();setBusy(true);const response=await fetch("/api/roles",{method:form.id?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const result=await response.json();setMessage(response.ok?`Role ${form.id?"updated":"created"}.`:result.message);if(response.ok){setForm(empty);await load();}else setBusy(false);} async function remove(role:Role){if(!confirm(`Delete ${role.name}?`))return;const response=await fetch("/api/roles",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:role.id})});const result=await response.json();setMessage(response.ok?"Role deleted.":result.message);if(response.ok)await load();} return <section className="mx-auto max-w-5xl space-y-6"><div><h1 className="text-2xl font-bold">Role Management</h1><p className="text-sm text-muted-foreground">Create roles with stable unique IDs and control administrative permissions.</p></div><form onSubmit={save} className="space-y-4 rounded-xl border bg-background p-4"><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm">Role name<Input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="text-sm">Status<select className="mt-1 w-full rounded-md border bg-background p-2" value={form.status} onChange={e=>setForm({...form,status:e.target.value as Role["status"]})}><option value="active">Active</option><option value="inactive">Inactive</option></select></label><label className="text-sm sm:col-span-2">Description<Input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label></div><div className="flex flex-wrap gap-4">{[["manageUsers","Manage users and master data"],["manageAttendance","Manage attendance"],["viewAttendanceReports","View attendance reports"]].map(([key,label])=><label key={key} className="flex gap-2 text-sm"><input type="checkbox" checked={Boolean(form[key as keyof Role])} onChange={e=>setForm({...form,[key]:e.target.checked})}/>{label}</label>)}</div><div className="flex gap-2"><Button disabled={busy}>{form.id?"Save role":"Add role"}</Button>{form.id&&<Button type="button" variant="ghost" onClick={()=>setForm(empty)}>Cancel</Button>}</div></form>{message&&<p className="rounded-md border p-3 text-sm">{message}</p>}<div className="overflow-x-auto rounded-xl border bg-background"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr>{["Role ID","Name","Permissions","Status","Actions"].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{roles.map(role=><tr key={role.id} className="border-t"><td className="p-3 font-mono text-xs">{role.id}</td><td className="p-3"><strong>{role.name}</strong><p className="text-xs text-muted-foreground">{role.description}</p></td><td className="p-3">{[role.manageUsers&&"Users",role.manageAttendance&&"Attendance",role.viewAttendanceReports&&"Attendance reports"].filter(Boolean).join(", ")||"Standard access"}</td><td className="p-3">{role.status}</td><td className="p-3"><div className="flex gap-2"><Button variant="outline" onClick={()=>setForm(role)}>Edit</Button><Button variant="outline" className="text-destructive" onClick={()=>void remove(role)}>Delete</Button></div></td></tr>)}</tbody></table></div></section>; }
